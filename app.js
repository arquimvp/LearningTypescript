//Funcion con parametros opcionales
//Se agrega un ? despues del nombre del parametro
function nombreCompleto(nombre, apellido) {
    return nombre + " " + apellido;
}
var nombre = nombreCompleto("juan");
console.log(nombre);
